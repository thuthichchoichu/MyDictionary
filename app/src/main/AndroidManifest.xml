<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.visualpro.realproject">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".views.MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_book_svgrepo_com"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_book_svgrepo_com"
        android:supportsRtl="true"
        android:theme="@style/Theme.mainActivityTheme">
        <activity
            android:name=".views.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".views.ResultActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity
            android:name=".views.MainActivity2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>